require 'spec_helper'
describe ApplicationController do
	describe "current_user" do
		it "redirect_to login page if id not found" do
			app = ApplicationController.new
			session[:user_id] = '1'
			app.instanceeval{current_user}
			app.should redirect_to('/login')
		end
	end
end
